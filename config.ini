# FastDevFs Configuration
# Generated automatically — edit with care

# Filesystem paths
data_dir = /tmp/fastdevfs_data
persist_path = /tmp/fastdevfs.mmap
dedup_path = /tmp/fastdevfs_dedup.mmap
socket_path = /tmp/fastdevfs_cli.sock
pid_path = /tmp/fastdevfs.pid
mountpoint = test_dir

# Deduplication
dedup_enabled = true
hash_algorithm = sha256
chunk_size = 4096

# Runtime
log_level = info
max_threads = 4
daemon_mode = false
